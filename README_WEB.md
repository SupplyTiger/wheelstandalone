# The Wheel Web App

The full setup, secrets, Postgres migration, SnapTrade/Fidelity sync, login flow, troubleshooting, and intern handoff guide now lives in:

```text
README.md
```

Keep this file only as a compatibility pointer for older notes that referenced `README_WEB.md`.
